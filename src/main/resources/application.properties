server.port=${PORT:8080}
server.address=0.0.0.0
server.servlet.context-path=/api
server.compression.enabled=true
server.compression.mime-types=text/html,text/xml,text/plain,text/css,text/javascript,application/javascript,application/json,application/xml
server.compression.min-response-size=1024


cors.allowed-origins=https://food-delivery-frontend-gzb9.vercel.app,http://localhost:5173,http://localhost:3000,https://food-delivery-frontend-kappa-rouge.vercel.app,https://food-delivery-frontend-git-main-priyanshus-projects-afa9bfcc.vercel.app

# MySQL Configuration (SQL - Transactional Data)
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3306/food_delivery_db?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:root}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:pop99558$$}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA Properties
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.open-in-view=false

# MongoDB Configuration (NoSQL - Catalog/Fast Reads)
spring.data.mongodb.uri=${MONGODB_URI:mongodb+srv://pv254424_db_user:s4VSChJ3XfgO0ms2@cluster0.kzeelbt.mongodb.net/food_delivery_db?appName=Cluster0}

# Logging
logging.level.com.fooddelivery=INFO
logging.level.org.springframework.security=WARN
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n

# JWT Configuration
app.jwt.secret=${JWT_SECRET:404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970}
app.jwt.expiration-ms=604800000
app.jwt.refresh-expiration-ms=1209600000

# Email Configuration
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME}
spring.mail.password=${MAIL_PASSWORD}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true

# Kafka Configuration
spring.kafka.bootstrap-servers=${KAFKA_BOOTSTRAP_SERVERS:localhost:9092}
spring.kafka.listener.auto-startup=${KAFKA_ENABLED:true}
spring.kafka.admin.fail-fast=false
spring.kafka.admin.auto-create=true

# Aiven Kafka SSL Configuration (Required for Aiven)
spring.kafka.properties.security.protocol=${KAFKA_SECURITY_PROTOCOL:PLAINTEXT}
spring.kafka.properties.ssl.endpoint.identification.algorithm=
# Add these in your environment variables if using Aiven:
# KAFKA_SECURITY_PROTOCOL=SSL
# spring.kafka.properties.ssl.truststore.location=/path/to/truststore.jks
# spring.kafka.properties.ssl.truststore.password=your_password

spring.kafka.producer.key-serializer=org.apache.kafka.common.serialization.StringSerializer
spring.kafka.producer.value-serializer=org.springframework.kafka.support.serializer.JsonSerializer
spring.kafka.consumer.group-id=email-service-group
spring.kafka.consumer.key-deserializer=org.apache.kafka.common.serialization.StringDeserializer
spring.kafka.consumer.value-deserializer=org.springframework.kafka.support.serializer.JsonDeserializer
spring.kafka.consumer.properties.spring.json.trusted.packages=com.delivery.foodDelivery.dto


# Valkey / Redis Configuration (Aiven Free Tier)
spring.data.redis.host=${VALKEY_HOST:localhost}
spring.data.redis.port=${VALKEY_PORT:6379}
spring.data.redis.password=${VALKEY_PASSWORD}
spring.data.redis.ssl.enabled=${VALKEY_SSL:false}

# Cache Configuration
spring.cache.type=${CACHE_TYPE:simple}

spring.cache.redis.time-to-live=600000
spring.cache.redis.cache-null-values=true

# Cloudinary Configuration
cloudinary.cloud_name=${CLOUDINARY_CLOUD_NAME}
cloudinary.api_key=${CLOUDINARY_API_KEY}
cloudinary.api_secret=${CLOUDINARY_API_SECRET}

# AI Assistant (Groq) Defaults
groq.api.key=${GROQ_API_KEY:}
groq.api.url=${GROQ_API_URL:https://api.groq.com/openai/v1/chat/completions}
groq.api.model=${GROQ_API_MODEL:llama-3.3-70b-versatile}

# Overriding
spring.main.allow-bean-definition-overriding=true

# Google OAuth
google.client.id=${GOOGLE_CLIENT_ID:}

